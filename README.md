# model-RSopen
Ruimtescanner Openbare versie

## Aan de slag

De machine-eigen paden (brondata, LocalData) staan in cfg/main/ConfigSettings.dms. Dat bestand staat niet in git. Kopieer cfg/main/ConfigSettings.dms.example naar cfg/main/ConfigSettings.dms en pas de paden aan voordat je de configuratie opent.
